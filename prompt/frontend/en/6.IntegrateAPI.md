# Integrate API

Connect the frontend to the **backend API**, replacing mock data from steps 3–5. Do not redesign UI layout unless needed for loading or error states — **preserve responsive behavior from steps 2–5**.

Before writing any code, read the following in detail, then briefly summarize which endpoint each page will call:

- `AcceptanceCriteria.md` — all of Stories 1–3
- `DefinitionOfDone.md` — filter, sort, validation, error handling, cover image editing
- API documentation in the project (e.g. `[path, e.g. /project/docs/API]`) — **source of truth for endpoints, requests, and responses**
- Frontend pages built in steps 3–5

If API docs are missing, endpoints do not cover AC/DoD, or responses conflict with the existing UI, ask first. Do not invent endpoints or fields that are not in the API docs.

## Scope

### 1. API layer

- Create a shared API layer (e.g. service or composable) for reuse across pages
- Make the base URL configurable (e.g. `http://localhost:8080`, path `/v1/api/...`)
- Handle requests/responses per API docs (query params, JSON, `multipart/form-data` for image uploads)

### 2. Home page (Story 1)

| Feature | Integration |
| --- | --- |
| Product list | Fetch from API instead of mock data |
| Filter | Send filter criteria to API (multiple filters at once) |
| Sort | Support DoD sort options: newest, price, most views |
| Line / phone buttons | Use data from API response |
| Navigation to detail / admin | Keep existing behavior |

### 3. Product detail (Story 2)

| Feature | Integration |
| --- | --- |
| Load product | Fetch by id from route |
| Gallery | Use image URLs from API |
| Vehicle / seller info / contact buttons | Use API response data |

### 4. Admin page (Story 3)

| Feature | Integration |
| --- | --- |
| Product table | Fetch from API |
| Create / edit / delete | Call API per documentation |
| Image upload | Send `multipart/form-data` per API spec |
| Cover image | Support DoD cover image editing for listings |
| Validation | Show API errors (e.g. duplicate title `409`, invalid price `400`) alongside existing client validation |

### 5. Error handling

- Handle loading states during API calls
- Show user-friendly error messages when the API fails or validation fails
- Handle not-found cases (e.g. missing product — `404`)

## Do not

- Design new endpoints — use only what exists in the API docs
- Refactor UI layout heavily unless necessary
- Remove mock data until all pages are fully replaced by API calls

## After completing

Provide a brief summary covering:

1. Files created or updated
2. Mapping table: page/feature → endpoint
3. How to configure the base URL and run frontend + backend together
4. Error cases now handled

If anything is unclear or you have any questions, always ask me before proceeding.
